# =============================================================================
# Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Domain Configuration
# -----------------------------------------------------------------------------
# Production: jwhitezaps.atoaz.com
# Local dev: localhost (handled by docker-compose.override.yml)
DOMAIN=jwhitezaps.atoaz.com

# Email for Let's Encrypt SSL certificate notifications
ACME_EMAIL=your-email@example.com

# -----------------------------------------------------------------------------
# Traefik Dashboard Authentication
# -----------------------------------------------------------------------------
# Generate with: echo $(htpasswd -nb admin your-password) | sed -e s/\\$/\\$\\$/g
# The sed command escapes $ for docker-compose
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$xyz...

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
# For local development (containerized PostgreSQL)
POSTGRES_USER=zapier
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=zapier_replacement

# For production with managed PostgreSQL (DigitalOcean), use the full connection string:
# DATABASE_URL=postgresql://user:password@host:port/database

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
# Default works for local containerized Redis
# For production managed Redis, update the URL
REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# Grafana
# -----------------------------------------------------------------------------
GRAFANA_USER=admin
GRAFANA_PASSWORD=your-grafana-password-here

# -----------------------------------------------------------------------------
# RingCentral API
# -----------------------------------------------------------------------------
# Get these from https://developers.ringcentral.com - your app settings
RINGCENTRAL_CLIENT_ID=your-client-id
RINGCENTRAL_CLIENT_SECRET=your-client-secret
# Create JWT at: Developer Console > Credentials > Create JWT Credential
RINGCENTRAL_JWT_TOKEN=your-jwt-token
RINGCENTRAL_SERVER_URL=https://platform.ringcentral.com

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
